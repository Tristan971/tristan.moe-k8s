apiVersion: v1
kind: Service
metadata:
  namespace: kmdah
  name: kmdah-mongodb-service
  labels:
    app.kubernetes.io/part-of: kmdah
spec:
  ports:
    - port: 27017
      name: mongodb
      targetPort: mongodb
  selector:
    app.kubernetes.io/name: kmdah-mongodb
