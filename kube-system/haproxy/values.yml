controller:
  kind: "DaemonSet"

  daemonset:
    useHostPort: "true"

  config:
    use-proxy-protocol: "true"
    forwardfor: "update"

  stats:
    enabled: "true"
  metrics:
    enabled: "true"
    port: "9101"

  logs:
    enabled: false

  podAnnotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9101"

  service:
    type: NodePort
